import pytest

# Import the session_fix fixture from session_scope_shared module
# This makes it available to all tests in this directory
from conftest_patterns.session_scope_shared import session_fix


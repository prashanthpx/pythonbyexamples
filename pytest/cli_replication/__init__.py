"""CLI and replication-style fixtures for pytest training.

The examples in this package show how to model a tiny command-line
interface (CLI) that talks to an in-memory "cluster" object, and how to
wire those pieces together with fixtures that resemble the
`cli`/replication fixtures from the real `clusterload` test suite.
"""

